package io.xoana.ikarus;

import koma.*;
import koma.matrix.Matrix;

import io.xoana.ikarus.nodes.*;

fun main(args: Array<String>) {
	println("Hello, world!");
	//val inp = InputNode(10, 10)
	//val n = UnaryNode((x:Double) -> { x*x }, (x:Double) -> { 2.0*x }, inp)
}
